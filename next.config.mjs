/** @type {import('next').NextConfig} */
const config = {
  output: 'export',
  basePath: '/port-react-folio',
  assetPrefix: '/port-react-folio/',
  trailingSlash: true,
  images: { unoptimized: true },
}

export default config

