# Run tests and build before push
npm run lint
npm run build

# Optional: Auto-release on push to main
# Uncomment the following lines to enable automatic versioning on push
# BRANCH=$(git rev-parse --abbrev-ref HEAD)
# if [ "$BRANCH" = "main" ]; then
#   echo "Creating new release version..."
#   npm run release
#   git push --follow-tags origin main
# fi